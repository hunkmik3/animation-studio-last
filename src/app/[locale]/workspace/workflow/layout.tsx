// =============================================
// Workflow Editor Layout — Full-screen, no navbar
// =============================================

export default function WorkflowLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return <>{children}</>
}
